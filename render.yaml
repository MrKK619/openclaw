services:
  - type: web
    name: openclaw
    runtime: docker
    dockerCommand: node openclaw.mjs gateway --allow-unconfigured --bind lan --port 8080
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: OPENCLAW_GATEWAY_PORT
        value: "8080"
      - key: OPENCLAW_STATE_DIR
        value: /data/.openclaw
      - key: OPENCLAW_WORKSPACE_DIR
        value: /data/workspace
      - key: OPENCLAW_GATEWAY_TOKEN
        generateValue: true
      - key: NODE_OPTIONS
        value: "--max-old-space-size=384"
      - key: OPENCLAW_ALLOWED_ORIGINS
        value: ""
      - key: OPENCLAW_DANGEROUS_ALLOW_HOST_HEADER_ORIGIN_FALLBACK
        value: "false"
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
